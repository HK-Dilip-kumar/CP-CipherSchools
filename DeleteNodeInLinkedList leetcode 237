class Solution {
public:
    void deleteNode(ListNode* node) {
        
        node->val = node->next->val; //just replace with next value
        node->next = node->next->next; //replace the next link
    }
};
